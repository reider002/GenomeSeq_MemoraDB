#@app.route('/api/reverse_complement/<sequence>')
#def reverse_complement(sequence):
    # Convert the sequence to a Seq object
 #   seq = Seq(sequence, generic_dna)
    
    # Get the reverse complement of the sequence
  #  rev_comp = seq.reverse_complement()
    
    # Return the reverse complement as a JSON object
   # return jsonify({'result': str(rev_comp)})
