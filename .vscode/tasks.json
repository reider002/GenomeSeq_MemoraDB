{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake: Build Memora Project",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build --config Debug --target all",
            "group": "build",
            "problemMatcher": []
        }
    ]
}